# Production container listens on this port.
PORT=3000
# Optional in local development; set to the exact public origin in Coolify.
# No trailing slash or path. The server normalizes it to an origin.
APP_ORIGIN=https://poker.example.com
