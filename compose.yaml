services:
  pokerplanner:
    build: .
    restart: unless-stopped
    ports:
      - '3000:3000'
    environment:
      PORT: '3000'
      APP_ORIGIN: ${APP_ORIGIN:-http://localhost:3000}
    init: true
    stop_grace_period: 10s
